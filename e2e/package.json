{
  "name": "petclinix-e2e",
  "version": "1.0.0",
  "description": "End-to-end tests for PetcliniX — runs against the full Docker Compose stack",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
